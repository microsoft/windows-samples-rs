[package]
name = "bindings"
version = "0.0.0"
edition = "2018"

[dependencies]
windows = { version = "0.29", features = ["build", "Foundation", "Graphics_DirectX_Direct3D11"] }

[build-dependencies]
windows = { version = "0.29", features = ["build"] }
